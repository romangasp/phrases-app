import type { FormInputsRenderProps } from "./types";

export const FormInputsRender = ({ filters }: FormInputsRenderProps) => {
  // {filters.map((filter) => {
  // if (filter.type === "text") {
  //   return (
  //     <div>
  //       <Input
  //         label={filter.label}
  //         placeholder={filter.placeholder}
  //         name={filter.name}
  //         value=""
  //         onChange={handleChange}
  //       />
  //     </div>
  //   );
  // }
  // })}
};
